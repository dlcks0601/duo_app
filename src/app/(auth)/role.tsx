import { AppText } from '@/components/AppText';
import { View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function RoleScreen() {
  return (
    <SafeAreaView>
      <View>
        <AppText>Role Screen</AppText>
      </View>
    </SafeAreaView>
  );
}
